# Production Environment Variables for Railway Deployment
# Copy these to your Railway project environment variables

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=your-super-secret-key-change-this-in-production
JWT_SECRET_KEY=your-jwt-secret-key-change-this-in-production
SECURITY_PASSWORD_SALT=your-security-salt-change-this-in-production

# Database (Railway will provide DATABASE_URL)
# DATABASE_URL will be automatically set by Railway PostgreSQL service

# Cache/Redis (if using Railway Redis)
# USE_REDIS=true
# REDIS_URL will be automatically set by Railway Redis service

# CORS Origins (update with your frontend domain)
CORS_ORIGINS=https://your-frontend-domain.railway.app,https://granula.app

# Application URLs
BASE_URL=https://your-backend-domain.railway.app
FRONTEND_URL=https://your-frontend-domain.railway.app

# JWT Security
JWT_COOKIE_SECURE=true
JWT_COOKIE_SAMESITE=None
JWT_COOKIE_CSRF_PROTECT=true

# Email Configuration (configure with your email provider)
MAIL_SERVER=smtp.sendgrid.net
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=apikey
MAIL_PASSWORD=your-sendgrid-api-key
MAIL_DEFAULT_SENDER=hello@granula.app

# Admin Configuration
ADMIN_EMAIL=admin@granula.app
SUPPORT_EMAIL=support@granula.app

# OAuth (if using Google OAuth)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# OAUTH_REDIRECT_URI=https://your-backend-domain.railway.app/api/auth/google/callback